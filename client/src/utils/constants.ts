export enum Class {
	CHAR_ERROR="text-red-400",
	CHAR_CORRECT = "text-gray-300",
	COLOR_TRANSITION = "transition-colors",
	WORD_INCORRECT = "underline-error"
}

export enum States {
	typing = "typing",
	result = "result"
}
